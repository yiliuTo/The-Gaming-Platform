server:
  port: 4700
  
spring:
  cloud:
    azure:
      credential:
        managed-identity-enabled: true
        client-id: ${AZURE_CLIENT_ID}
      servicebus:
        namespace: "localhost"
        entity-type: queue
        processor:
          max-concurrent-calls: 1
          prefetch-count: 1
  data:
    mongodb:
      uri: mongodb+srv://Omar:KYCcRa1SwN5stM0n@cluster0.yuezt.mongodb.net/PostMS?retryWrites=true&w=majority