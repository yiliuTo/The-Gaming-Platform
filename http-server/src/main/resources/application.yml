spring:
  rabbitmq:
    channel-rpc-timeout: 10000
  cloud:
    azure:
      credential:
        managed-identity-enabled: true
        client-id: ${AZURE_CLIENT_ID}
      servicebus:
        namespace: "localhost"
        entity-type: queue
        consumer:
          session-enabled: true


